apiVersion: v1
kind: Service
metadata:
  name: {{ include "cal-helm.fullname" . }}
  labels:
    {{- include "cal-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cal-helm.selectorLabels" . | nindent 4 }}
